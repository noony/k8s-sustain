---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: policies.k8s.sustain.io
spec:
  group: k8s.sustain.io
  names:
    kind: Policy
    listKind: PolicyList
    plural: policies
    singular: policy
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Policy is the Schema for the policies API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PolicySpec defines the desired state of a Policy.
            properties:
              rightSizing:
                description: RightSizing configures resource recommendation and application.
                properties:
                  autoscalerCoordination:
                    description: AutoscalerCoordination configures HPA/ScaledObject-aware
                      request shaping.
                    properties:
                      enabled:
                        description: Enabled turns on the overhead formula for any
                          resource the autoscaler targets on averageUtilization.
                        type: boolean
                      replicaBudgetAnchor:
                        description: ReplicaBudgetAnchor is the fraction into [minReplicas,
                          maxReplicas] the workload should sit at steady state (typically
                          0.10); nil disables replica correction.
                        maximum: 1
                        minimum: 0
                        type: number
                    type: object
                  excludeInitContainers:
                    description: ExcludeInitContainers skips init containers, including
                      restartable sidecars, for every workload this policy targets.
                    type: boolean
                  recommendOnly:
                    description: RecommendOnly puts every workload governed by this
                      policy in dry-run; it is ORed with the global --recommend-only
                      flag.
                    type: boolean
                  resourcesConfigs:
                    description: ResourcesConfigs holds per-resource-dimension recommendation
                      configs.
                    properties:
                      cpu:
                        description: CPU holds the recommendation config for CPU resources.
                        properties:
                          downsizeThreshold:
                            description: DownsizeThreshold suppresses decreases smaller
                              than max(percent% of current, minDecrease); defaults
                              to 5% and 10m CPU / 15Mi memory when unset.
                            properties:
                              minDecrease:
                                anyOf:
                                - type: integer
                                - type: string
                                description: MinDecrease is the minimum decrease as
                                  an absolute quantity (default 10m for CPU, 15Mi
                                  for memory).
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              percent:
                                description: Percent is the minimum decrease as a
                                  percentage of the current value (default 5).
                                format: int32
                                maximum: 100
                                minimum: 0
                                type: integer
                            type: object
                          limits:
                            description: Limits configures how resource limits are
                              derived from requests.
                            properties:
                              equalsToRequest:
                                description: EqualsToRequest sets the limit equal
                                  to the computed request.
                                type: boolean
                              keepLimit:
                                description: KeepLimit leaves the existing limit unchanged.
                                type: boolean
                              keepLimitRequestRatio:
                                description: KeepLimitRequestRatio preserves the current
                                  limit-to-request ratio.
                                type: boolean
                              noLimit:
                                description: NoLimit removes the limit entirely.
                                type: boolean
                              requestsLimitsRatio:
                                description: RequestsLimitsRatio sets the limit as
                                  a multiple (>= 1) of the request.
                                minimum: 1
                                type: number
                            type: object
                            x-kubernetes-validations:
                            - message: at most one of equalsToRequest, keepLimit,
                                keepLimitRequestRatio, noLimit, requestsLimitsRatio
                                may be set
                              rule: '((has(self.equalsToRequest) && self.equalsToRequest)
                                ? 1 : 0) + ((has(self.keepLimit) && self.keepLimit)
                                ? 1 : 0) + ((has(self.keepLimitRequestRatio) && self.keepLimitRequestRatio)
                                ? 1 : 0) + ((has(self.noLimit) && self.noLimit) ?
                                1 : 0) + (has(self.requestsLimitsRatio) ? 1 : 0) <=
                                1'
                          requests:
                            description: Requests configures how resource requests
                              are computed.
                            properties:
                              headroom:
                                description: Headroom adds a safety buffer on top
                                  of the computed recommendation (percentage, 0-100).
                                format: int32
                                maximum: 100
                                minimum: 0
                                type: integer
                              keepRequest:
                                description: KeepRequest disables request updates
                                  when true.
                                type: boolean
                              maxAllowed:
                                anyOf:
                                - type: integer
                                - type: string
                                description: MaxAllowed caps the recommended request
                                  value.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              minAllowed:
                                anyOf:
                                - type: integer
                                - type: string
                                description: MinAllowed floors the recommended request
                                  value.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              percentile:
                                description: Percentile is the histogram percentile
                                  used for the recommendation (e.g. 95); 100 resolves
                                  to the maximum sample over the window.
                                format: int32
                                maximum: 100
                                minimum: 1
                                type: integer
                            type: object
                            x-kubernetes-validations:
                            - message: minAllowed must be less than or equal to maxAllowed
                              rule: '!has(self.minAllowed) || !has(self.maxAllowed)
                                || quantity(self.minAllowed).compareTo(quantity(self.maxAllowed))
                                <= 0'
                            - message: keepRequest cannot be combined with headroom,
                                percentile, minAllowed, or maxAllowed (they have no
                                effect when the request is kept)
                              rule: '!(has(self.keepRequest) && self.keepRequest)
                                || (!has(self.headroom) && !has(self.percentile) &&
                                !has(self.minAllowed) && !has(self.maxAllowed))'
                          window:
                            description: Window is the Prometheus duration of the
                              observation window used for recommendation (e.g. "96h").
                            pattern: ^([0-9]+(m|h|d|w|y))+$
                            type: string
                        type: object
                      memory:
                        description: Memory holds the recommendation config for memory
                          resources.
                        properties:
                          downsizeThreshold:
                            description: DownsizeThreshold suppresses decreases smaller
                              than max(percent% of current, minDecrease); defaults
                              to 5% and 10m CPU / 15Mi memory when unset.
                            properties:
                              minDecrease:
                                anyOf:
                                - type: integer
                                - type: string
                                description: MinDecrease is the minimum decrease as
                                  an absolute quantity (default 10m for CPU, 15Mi
                                  for memory).
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              percent:
                                description: Percent is the minimum decrease as a
                                  percentage of the current value (default 5).
                                format: int32
                                maximum: 100
                                minimum: 0
                                type: integer
                            type: object
                          limits:
                            description: Limits configures how resource limits are
                              derived from requests.
                            properties:
                              equalsToRequest:
                                description: EqualsToRequest sets the limit equal
                                  to the computed request.
                                type: boolean
                              keepLimit:
                                description: KeepLimit leaves the existing limit unchanged.
                                type: boolean
                              keepLimitRequestRatio:
                                description: KeepLimitRequestRatio preserves the current
                                  limit-to-request ratio.
                                type: boolean
                              noLimit:
                                description: NoLimit removes the limit entirely.
                                type: boolean
                              requestsLimitsRatio:
                                description: RequestsLimitsRatio sets the limit as
                                  a multiple (>= 1) of the request.
                                minimum: 1
                                type: number
                            type: object
                            x-kubernetes-validations:
                            - message: at most one of equalsToRequest, keepLimit,
                                keepLimitRequestRatio, noLimit, requestsLimitsRatio
                                may be set
                              rule: '((has(self.equalsToRequest) && self.equalsToRequest)
                                ? 1 : 0) + ((has(self.keepLimit) && self.keepLimit)
                                ? 1 : 0) + ((has(self.keepLimitRequestRatio) && self.keepLimitRequestRatio)
                                ? 1 : 0) + ((has(self.noLimit) && self.noLimit) ?
                                1 : 0) + (has(self.requestsLimitsRatio) ? 1 : 0) <=
                                1'
                          requests:
                            description: Requests configures how resource requests
                              are computed.
                            properties:
                              headroom:
                                description: Headroom adds a safety buffer on top
                                  of the computed recommendation (percentage, 0-100).
                                format: int32
                                maximum: 100
                                minimum: 0
                                type: integer
                              keepRequest:
                                description: KeepRequest disables request updates
                                  when true.
                                type: boolean
                              maxAllowed:
                                anyOf:
                                - type: integer
                                - type: string
                                description: MaxAllowed caps the recommended request
                                  value.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              minAllowed:
                                anyOf:
                                - type: integer
                                - type: string
                                description: MinAllowed floors the recommended request
                                  value.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              percentile:
                                description: Percentile is the histogram percentile
                                  used for the recommendation (e.g. 95); 100 resolves
                                  to the maximum sample over the window.
                                format: int32
                                maximum: 100
                                minimum: 1
                                type: integer
                            type: object
                            x-kubernetes-validations:
                            - message: minAllowed must be less than or equal to maxAllowed
                              rule: '!has(self.minAllowed) || !has(self.maxAllowed)
                                || quantity(self.minAllowed).compareTo(quantity(self.maxAllowed))
                                <= 0'
                            - message: keepRequest cannot be combined with headroom,
                                percentile, minAllowed, or maxAllowed (they have no
                                effect when the request is kept)
                              rule: '!(has(self.keepRequest) && self.keepRequest)
                                || (!has(self.headroom) && !has(self.percentile) &&
                                !has(self.minAllowed) && !has(self.maxAllowed))'
                          window:
                            description: Window is the Prometheus duration of the
                              observation window used for recommendation (e.g. "96h").
                            pattern: ^([0-9]+(m|h|d|w|y))+$
                            type: string
                        type: object
                    type: object
                  update:
                    description: Update configures which workload types are reconciled
                      and how.
                    properties:
                      eviction:
                        description: Eviction controls eviction behaviour during right-sizing.
                        properties:
                          ignoreAutoscalerSafeToEvictAnnotations:
                            description: 'IgnoreAutoscalerSafeToEvictAnnotations evicts
                              pods even when they carry cluster-autoscaler.kubernetes.io/safe-to-evict:
                              "false".'
                            type: boolean
                        type: object
                      types:
                        description: Types lists the workload types and their update
                          modes.
                        properties:
                          argoRollout:
                            description: UpdateMode defines how resources are updated
                              on a given workload type.
                            enum:
                            - OnCreate
                            - Ongoing
                            type: string
                          cronJob:
                            description: 'CronJob never mutates the CronJob spec:
                              OnCreate injects resources at pod admission, Ongoing
                              additionally resizes running job pods in place.'
                            enum:
                            - OnCreate
                            - Ongoing
                            type: string
                          daemonSet:
                            description: UpdateMode defines how resources are updated
                              on a given workload type.
                            enum:
                            - OnCreate
                            - Ongoing
                            type: string
                          deployment:
                            description: UpdateMode defines how resources are updated
                              on a given workload type.
                            enum:
                            - OnCreate
                            - Ongoing
                            type: string
                          job:
                            description: UpdateMode defines how resources are updated
                              on a given workload type.
                            enum:
                            - OnCreate
                            - Ongoing
                            type: string
                          pod:
                            description: 'Pod targets bare pods that opt in via the
                              owner-name annotation: OnCreate injects resources at
                              admission, Ongoing additionally resizes running pods
                              in place (k8s >= 1.33); bare pods are never evicted.'
                            enum:
                            - OnCreate
                            - Ongoing
                            type: string
                          statefulSet:
                            description: UpdateMode defines how resources are updated
                              on a given workload type.
                            enum:
                            - OnCreate
                            - Ongoing
                            type: string
                        type: object
                    type: object
                type: object
              selector:
                description: Selector defines which namespaces and workloads this
                  policy applies to.
                properties:
                  labelSelector:
                    description: LabelSelector restricts the set of workloads targeted
                      by this policy; an empty selector matches all.
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                  namespaces:
                    description: Namespaces is a list of namespaces to target; an
                      empty list targets all namespaces.
                    items:
                      type: string
                    type: array
                type: object
            type: object
          status:
            description: PolicyStatus defines the observed state of a Policy.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the Policy state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
